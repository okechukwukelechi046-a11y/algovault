Using Algorithms in Your Code

from algorithms.graph.dijkstra import Graph, DijkstraSolver

# Create graph
graph = Graph(5, directed=True)
graph.add_edge(0, 1, 4)
graph.add_edge(0, 2, 2)
graph.add_edge(1, 3, 5)
graph.add_edge(2, 3, 8)
graph.add_edge(3, 4, 2)

# Find shortest paths
solver = DijkstraSolver(graph)
result = solver.solve(0)
print(f"Distance to vertex 4: {result['distances'][4]}")

# Get path
path = solver.get_path(4)
print(f"Path: {path}")
